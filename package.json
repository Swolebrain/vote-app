{
  "name": "vote_app",
  "version": "1.0.0",
  "author": "Fullstack.io",
  "scripts": {
    "go": "open http://localhost:3000; npm run server",
    "server": "echo 'Find the server at http://localhost:3000' && http-server -p 3000"
  },
  "private": true,
  "dependencies": {
    "http-server": "0.8.5"
  }
}
